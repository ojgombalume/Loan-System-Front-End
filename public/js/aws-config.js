// AWS Amplify Configuration
// This file will be automatically generated by Amplify CLI
// Update these values after running 'amplify push'

const awsConfig = {
  // Auth configuration (if using Cognito - optional)
  Auth: {
    region: 'us-east-1', // Change to your region
    userPoolId: 'YOUR_USER_POOL_ID', // Will be generated by Amplify
    userPoolWebClientId: 'YOUR_APP_CLIENT_ID', // Will be generated by Amplify
  },
  
  // API Gateway configuration
  API: {
    endpoints: [
      {
        name: 'SkycapAPI',
        endpoint: 'YOUR_API_GATEWAY_ENDPOINT', // Will be generated by Amplify
        region: 'us-east-1' // Change to your region
      }
    ]
  },
  
  // S3 Storage configuration
  Storage: {
    AWSS3: {
      bucket: 'YOUR_BUCKET_NAME', // Will be generated by Amplify
      region: 'us-east-1' // Change to your region
    }
  }
};

// For local development, you can override with local API
if (window.location.hostname === 'localhost' || window.location.hostname === '127.0.0.1') {
  // Uncomment for local development
  // awsConfig.API.endpoints[0].endpoint = 'http://localhost:3000/api';
}

// Export configuration
if (typeof module !== 'undefined' && module.exports) {
  module.exports = awsConfig;
}
